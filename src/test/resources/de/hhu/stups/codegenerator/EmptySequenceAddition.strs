fn main() {
    let mut empty = EmptySequence::new();
    empty.calculate();
    let res = empty.getRes();
    if res.card().equal(&0) {
        println!("{{}}");
    } else {
        println!("Wrong result!");
    }
}
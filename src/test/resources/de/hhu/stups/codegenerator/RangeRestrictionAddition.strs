fn main() {
    let mut range = RangeRestriction::new();
    range.calculate();
    let res =  range.getRes();
    if res.card() == 1 && res.functionCall(&EINT::ONE).eq(&EINT::TWO) {
        println!("{{(1 |-> 2)}}");
    } else {
        println!("Wrong result!");
    }
}
fn main() {
    let mut set = SetComprehension3::new();
    set.calculate();
    let mut res = set.getRes();
    if res.card().eq(&1) && res.iter().next().unwrap().eq(&3) {
        println!("{{3}}");
    } else {
        println!("Wrong result!: {:?}", res);
    }
}
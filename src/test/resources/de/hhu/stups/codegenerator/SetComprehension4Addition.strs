fn main() {
    let mut set = SetComprehension4::new();
    set.calculate();
    let mut res = set.getRes();
    if res.card().eq(&0) {
        println!("{{}}");
    } else {
        println!("Wrong result!: {:?}", res);
    }
}
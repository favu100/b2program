fn main() {
    let mut projection = TupleProjection2::new();
    projection.calculate();
    println!("{} ", projection.getRes());
    let pair = projection.getPair();
    println!("({} |-> {})", pair.projection1(), pair.projection2());
}
int main() {
    COUNT counter;
    counter.COUNT_clock_step(BInteger(1));
    cout << counter.getRes() << "\n";
    counter.COUNT_clock_step(BInteger(2));
    cout << counter.getRes() << "\n";
    for(int i = 0; i < 100; i++) {
        counter.COUNT_clock_step(BInteger(1));
    }
    cout << counter.getRes() << "\n";
    return 0;
}
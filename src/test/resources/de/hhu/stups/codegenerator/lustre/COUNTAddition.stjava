public static void main(String[] args) {
    COUNT counter = new COUNT();
    counter.COUNT_clock_step(new BInteger(1));
    System.out.println(counter.getRes());
    counter.COUNT_clock_step(new BInteger(2));
    System.out.println(counter.getRes());
    for(int i = 0; i < 100; i++) {
        counter.COUNT_clock_step(new BInteger(1));
    }
    System.out.println(counter.getRes());
}
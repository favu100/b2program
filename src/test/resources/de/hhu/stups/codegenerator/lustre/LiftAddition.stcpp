int main() {
    Lift lift;
    lift.Lift_clock_step(BBoolean(true), BBoolean(false));
    cout << lift.getFloor() << "\n";
    lift.Lift_clock_step(BBoolean(false), BBoolean(true));
    cout << lift.getFloor() << "\n";
    for(int i = 0; i < 100; i++) {
        lift.Lift_clock_step(BBoolean(true), BBoolean(false));
    }
    cout << lift.getFloor() << "\n";
    lift.Lift_clock_step(BBoolean(true), BBoolean(false));
    cout << lift.getFloor() << "\n";
    for(int i = 0; i < 100; i++) {
        lift.Lift_clock_step(BBoolean(false), BBoolean(true));
    }
    cout << lift.getFloor() << "\n";
    lift.Lift_clock_step(BBoolean(false), BBoolean(true));
    cout << lift.getFloor() << "\n";
    return 0;
}
public static void main(String[] args) {
    Lift_verif_error lift = new Lift_verif_error();
    lift.Lift_verif_error_clock_step(new BBoolean(true), new BBoolean(false));
    System.out.println(lift.getOK());
    for(int i = 0; i < 100; i++) {
        lift.Lift_verif_error_clock_step(new BBoolean(true), new BBoolean(false));
    }
    System.out.println(lift.getOK());
}